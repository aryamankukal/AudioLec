function startRecording() {
    let button = document.getElementById('recordingButton')
    button.classList.toggle("Rec")

    // console.log(`reached here`);
    // const button2 = document.createElement("button");
    // var buttonnode = document.createTextNode("Proceed");
    // button2.appendChild(buttonnode);

    // var holderdiv = document.getElementById('holder');
    // holderdiv.appendChild(button2)
}
